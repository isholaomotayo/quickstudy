describe('visit index page', () => {
    it('returns the homepage', () => {
        cy.visit('http://localhost:8080/');

        // cy.contains('Go').click();

        // check that page lopads
        // chrxck that every element the user interacts with works
        // check that when user fills forms it should work
    });
})